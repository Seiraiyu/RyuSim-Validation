[pytest]
testpaths =
    rtlmeter
    cocotb
    tests
markers =
    benchmark: Real-world processor benchmark tests
    sv_construct: SystemVerilog construct coverage tests
    expect_fail: Tests expected to fail (unsupported constructs)
    level1: Compile + cocotb pass validation
    level2: VCD comparison validation
